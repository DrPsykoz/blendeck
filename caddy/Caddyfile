blendeck.fr {
    # Auto Let's Encrypt — no config needed!

    # Backend API (SSE support for analysis/mix progress)
    handle_path /api/* {
        reverse_proxy backend:8000 {
            flush_interval -1
        }
    }

    # Frontend
    handle {
        reverse_proxy frontend:3000
    }
}
